# Full project: https://gitlab.com/pages/doxygen
image: ubuntu

pages:
  script:
  - apk update && apk add doxygen
  - doxygen Doxyfile
  - mv doc/html/ public/
  artifacts:
    paths:
    - public
  only:
  - master
